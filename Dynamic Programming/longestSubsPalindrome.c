#include<stdio.h>
char* LongSubsPal(char* st, int n)
{
    
}
int main()
{
    char st[100];
    scanf("%[^\n]%*c", st);
    int n= strlen(st);
    // printf("%d ",n);
    printf(" %s\ns",LongSubsPal(st,n));
    return 0;
}